import { Container, Row, Col } from "reactstrap"; //import the Reactstrap Components

const Home = () => {
  return (
    <div>
      <p>Home page rendered.</p>
    </div>
  );
};

export default Home;
